<UserControl x:Class="NoeticTools.TeamStatusBoard.TeamCity.Tiles.TeamCity.AgentStatus.TeamCityAgentStatusTileControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-NoeticTools.TeamStatusBoard.Framework.Converters"
             xmlns:loc="clr-namespace:NoeticTools.TeamStatusBoard.TeamCity.DataSources.TeamCity.Agents"
             xmlns:framework="clr-namespace:NoeticTools.TeamStatusBoard.Framework;assembly=NoeticTools.TeamStatusBoard.Framework"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             Name="TeamCityBuildStatus">

    <UserControl.Resources>

        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/NoeticTools.TeamStatusBoard.Framework;component/Styles/Style1.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
        
    </UserControl.Resources>

    <Border Name="FocusBorder" Style="{StaticResource FocusBorder}" Focusable="True">
        <Border.Resources>

            <Style x:Key="DeviceText" TargetType="{x:Type TextBlock}">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Path=BuildAgent.Status}" Value="{x:Static framework:DeviceStatus.Disabled}">
                        <Setter Property="Foreground" Value="White"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Path=BuildAgent.Status}" Value="{x:Static framework:DeviceStatus.Running}">
                        <Setter Property="Foreground" Value="DarkSlateGray"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Path=BuildAgent.Status}" Value="{x:Static framework:DeviceStatus.Idle}">
                        <Setter Property="Foreground" Value="White"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Path=BuildAgent.Status}" Value="{x:Static framework:DeviceStatus.Unknown}">
                        <Setter Property="Foreground" Value="White"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Path=BuildAgent.Status}" Value="{x:Static framework:DeviceStatus.Offline}">
                        <Setter Property="Foreground" Value="White"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Path=BuildAgent.Status}" Value="{x:Static framework:DeviceStatus.NotAuthorised}">
                        <Setter Property="Foreground" Value="White"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="DeviceBackground" TargetType="{x:Type Grid}">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Path=BuildAgent.Status}" Value="{x:Static framework:DeviceStatus.Disabled}">
                        <Setter Property="Background" Value="Gray"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Path=BuildAgent.Status}" Value="{x:Static framework:DeviceStatus.Running}">
                        <Setter Property="Background" Value="Yellow"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Path=BuildAgent.Status}" Value="{x:Static framework:DeviceStatus.Idle}">
                        <Setter Property="Background" Value="#FF448032"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Path=BuildAgent.Status}" Value="{x:Static framework:DeviceStatus.Unknown}">
                        <Setter Property="Background" Value="#FF292929"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Path=BuildAgent.Status}" Value="{x:Static framework:DeviceStatus.Offline}">
                        <Setter Property="Background" Value="Firebrick"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Path=BuildAgent.Status}" Value="{x:Static framework:DeviceStatus.NotAuthorised}">
                        <Setter Property="Background" Value="DarkCyan"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="BuildAgentImage" TargetType="{x:Type Image}">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Path=BuildAgent.IsOnline}" Value="True">
                        <Setter Property="Source" Value="/NoeticTools.TeamStatusBoard.Framework;component/Images/server128x128.png" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Path=BuildAgent.IsOnline}" Value="False">
                        <Setter Property="Source" Value="/NoeticTools.TeamStatusBoard.Framework;component/Images/server128x128_light.png" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>

        </Border.Resources>
        
        <Grid Name="root" Margin="0" Style="{StaticResource DeviceBackground}">
            
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*" MinHeight="20"/>
                <RowDefinition Height="3*"/>
            </Grid.RowDefinitions>
            
            <Image Name="agentImage" Grid.Row="0" Grid.RowSpan="3" Source="/NoeticTools.TeamStatusBoard.Framework;component/Images/server128x128.png" 
                   Opacity="0.9" MaxWidth="48" MaxHeight="48" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="15,15,10,10"
                   Style="{DynamicResource BuildAgentImage}" />

            <TextBlock Name="headerText" HorizontalAlignment="Center" VerticalAlignment="Top" Grid.Row="0"
                       Style="{DynamicResource DeviceText}"
                       FontSize="{Binding Path=ActualWidth, ElementName=root, 
                                Converter={StaticResource doubleConverter}, ConverterParameter=0.08}"
                       FontWeight="Bold" FontStyle="Italic" Margin="5" Text="{Binding BuildAgent.StatusText, Mode=OneWay}"/>

            <TextBlock Name="agentName" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="1"
                       Style="{DynamicResource DeviceText}"
                       FontSize="{Binding Path=ActualWidth, ElementName=root, 
                                Converter={StaticResource doubleConverter}, ConverterParameter=0.09}"
                       FontWeight="Bold" Margin="5" Text="{Binding BuildAgent.Name, Mode=OneWay}"/>
            
        </Grid>
    </Border>
</UserControl>